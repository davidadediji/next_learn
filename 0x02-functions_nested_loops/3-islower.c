#include "main.h"
/**
 * _islower - checks for lowercase
 *
 * Return: Always 1 or 0
 */
int _islower(int c)
{
	i = _islower(int c);

	if (i >= 'a' && i <= 'z')
	{
		return (1);
	}

	else 
	{
		return (0);
	}
}

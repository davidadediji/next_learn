#include "main.h"
/**
 *_sqrt_recursion - function to print a string in reverse
 *@n:unsigned character
 *Return:int-type
 */
int _sqrt_recursion(int n)
{
	unsigned int i;

	if (n < 0 || n == 0 || n == 1)
		return (0);
	if (i * i == n)
	{
		return (i);
	}
}

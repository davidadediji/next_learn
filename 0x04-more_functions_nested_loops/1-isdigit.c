#include "main.h"
/**
 * _isdigit - check for integers
 * @c: variable required
 * Return: Always 0 or 1
 */
int _isdigit(int c)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		if (c != 1)
		{
			return (0);
		}
		else
		{
			return (1);
		}
	}
}
